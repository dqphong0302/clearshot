{
  "name": "clearshot",
  "version": "1.0.0",
  "description": "Client-side image metadata (EXIF/XMP/IPTC/C2PA) remover. No uploads, no server.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "exifr": "^7.1.3"
  }
}
